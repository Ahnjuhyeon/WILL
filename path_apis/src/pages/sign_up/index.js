const SignUpPage = () => {
  //const navigate = useNavigate()
  const onSignup = async () => {
    /*
        try{
            await axiosInstance.post('/sign-up',form)
        navigate("/login")
        }
        catch(err){
            alert("id와 ....")
        }
        */
  };
  return <></>;
};
export default SignUpPage;

/*

1. 컴포넌트 구조 만들기 및 설계 //어떻게 나눌지 (공용으로 쓰일 것, 버튼, 인풋 등등)
2. 인풋과 아웃풋 생각 성공했을 때, 로그인 폼으로 이동 실패했을 때 에러 메세지 
   //에러메세지라면 백엔드에서 제공한걸 사용할 건지 내가 만들어서 사용할지 인지하고 있어야 한다.
   
*/
